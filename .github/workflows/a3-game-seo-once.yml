name: Canonical game metadata alignment

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/a3-game-seo-once.yml

permissions:
  contents: write

jobs:
  align:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Align metadata
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          title = 'Paradigm Shift | Joe Nasr'
          description = 'Paradigm Shift is Joe Nasr’s 3D perspective puzzle game about revealing hidden forms by changing the viewing angle.'
          canonical = 'https://joenasr.itch.io/paradigm-shift'
          repo = 'https://github.com/Joenasriani/paradigm-shift-game'
          person = 'https://joe-nasr-signals.vercel.app/v2/#joe-nasr'

          s = re.sub(r'<title\b[^>]*>.*?</title>', f'<title>{title}</title>', s, count=1, flags=re.I|re.S)
          start = '<!-- A3: Joe Nasr canonical game metadata -->'
          end = '<!-- /A3: Joe Nasr canonical game metadata -->'
          if start in s and end in s:
              s = s[:s.index(start)] + s[s.index(end) + len(end):]

          block = f'''{start}
          <meta name="description" content="{description}">
          <meta name="author" content="Joe Nasr">
          <meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1">
          <link rel="canonical" href="{canonical}">
          <meta property="og:type" content="website">
          <meta property="og:title" content="{title}">
          <meta property="og:description" content="{description}">
          <meta property="og:url" content="{canonical}">
          <meta name="twitter:card" content="summary_large_image">
          <meta name="twitter:title" content="{title}">
          <meta name="twitter:description" content="{description}">
          <script type="application/ld+json">
          {{
            "@context":"https://schema.org",
            "@type":"VideoGame",
            "@id":"{canonical}#game",
            "name":"Paradigm Shift",
            "url":"{canonical}",
            "description":"{description}",
            "genre":"Puzzle",
            "gamePlatform":"Web browser",
            "inLanguage":"en",
            "creator":{{
              "@type":"Person",
              "@id":"{person}",
              "name":"Joe Nasr",
              "alternateName":["Joe Ribal Nasr","Joseph Ribal Nasr"],
              "url":"https://joe-nasr-signals.vercel.app/v2/"
            }},
            "codeRepository":"{repo}"
          }}
          </script>
          {end}'''
          if '</head>' not in s.lower():
              raise SystemExit('No closing head tag found')
          m = re.search(r'</head>', s, flags=re.I)
          s = s[:m.start()] + block + '\n' + s[m.start():]
          p.write_text(s, encoding='utf-8')
          PY
          rm -f .github/workflows/a3-game-seo-once.yml
          git config user.name "Joe Nasr repository maintenance"
          git config user.email "actions@users.noreply.github.com"
          git add index.html .github/workflows/a3-game-seo-once.yml
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Align game metadata with canonical Joe Nasr identity"
          git push
